spring.application.name=user-service
spring.profiles.active=dev
spring.config.import=configserver:http://localhost:8888

# ============ Security defaults ============
# Ensures JWT interceptor beans are always created during component scanning.
# Config server values (config/dev/user-service.properties) override these at runtime.
security.jwt.enabled=true