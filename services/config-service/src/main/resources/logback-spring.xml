<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Spring Boot defaults: registers %clr, %wEx, sets CONSOLE_LOG_PATTERN property -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Custom per-level colour: ERROR=bold red, WARN=yellow, INFO=green, DEBUG=white -->
    <conversionRule conversionWord="lvl" class="com.booking.platform.common.logging.LevelColourConverter"/>

    <springProperty name="LOG_FILE" source="logging.file.name" defaultValue="logs/app.log"/>
    <springProperty name="APP_NAME" source="spring.application.name" defaultValue="app"/>

    <!-- Console: coloured by level for developer terminal use -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %lvl(%-5level [%X{correlationId:-},%X{traceId:-},%X{spanId:-}] %-40.40logger{39} : %m%n%wEx)"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- JSON file: structured logs for Loki/Grafana -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${APP_NAME}"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="io.grpc" level="WARN"/>
    <logger name="com.netflix.eureka" level="WARN"/>
    <logger name="com.netflix.discovery" level="WARN"/>
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="com.booking" level="DEBUG"/>

</configuration>
