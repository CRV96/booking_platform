spring.application.name=event-service
spring.profiles.active=dev
spring.config.import=configserver:${CONFIG_SERVER_URL:http://localhost:8888}

# ============ Security defaults ============
# Ensures JWT interceptor beans are always created during component scanning.
# Config server values (config/dev/event-service.properties) override these at runtime.
security.jwt.enabled=true