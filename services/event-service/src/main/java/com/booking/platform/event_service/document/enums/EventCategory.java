package com.booking.platform.event_service.document.enums;

/**
 * Enum representing the various categories an event can belong to.
 */
public enum EventCategory {
    CONCERT,
    SPORTS,
    THEATRE,
    CONFERENCE,
    FESTIVAL,
    OTHER
}
