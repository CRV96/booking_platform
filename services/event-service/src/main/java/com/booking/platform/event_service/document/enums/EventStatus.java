package com.booking.platform.event_service.document.enums;

/**
 * Enum representing the various statuses an event can have in the system.
 */
public enum EventStatus {
    DRAFT,
    PUBLISHED,
    CANCELLED,
    COMPLETED
}
