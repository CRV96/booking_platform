apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  namespace: booking-platform
type: Opaque
stringData:
  NOTIFICATION_SERVICE_KEYCLOAK_CLIENT_SECRET: ${NOTIFICATION_SERVICE_KEYCLOAK_CLIENT_SECRET}
