apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # bkg-prometheus is the Docker container name — Grafana resolves it
    # via the shared bkg-network Docker network
    url: http://bkg-prometheus:9090
    access: proxy
    isDefault: true
    editable: true
